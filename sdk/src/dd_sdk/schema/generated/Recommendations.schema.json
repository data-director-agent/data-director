{
   "$comment": "Generated from data_director.yaml by `uv run dd-gen-schema`. Do not edit.",
   "$defs": {
      "Derivation": {
         "description": "",
         "enum": [
            "template",
            "model",
            "lexical",
            "registry"
         ],
         "title": "Derivation",
         "type": "string"
      },
      "GroundingRef": {
         "additionalProperties": false,
         "description": "The identity and content hash of one retrieved thing a payload rests on. The only way a payload may name an external resource as a decided identity, and the only place it names it: what the reference identifies is read from the evidence item with the same source_id and content_hash (ADR-0015). The linter matches every GroundingRef against a retrieval span on both fields.",
         "properties": {
            "source_id": {
               "type": "string"
            },
            "content_hash": {
               "pattern": "^[0-9a-f]{64}$",
               "type": "string"
            }
         },
         "required": [
            "source_id",
            "content_hash"
         ],
         "title": "GroundingRef",
         "type": "object"
      },
      "Recommendation": {
         "additionalProperties": false,
         "description": "One recommended resource for one target. The resource is named only in grounded_on; its name, status and DOI are in the cited evidence item's content (ADR-0015).",
         "properties": {
            "kind": {
               "$ref": "#/$defs/RecommendationKind"
            },
            "target": {
               "description": "dataset, or field:<name> for a field-level recommendation.",
               "type": "string"
            },
            "score": {
               "type": [
                  "number",
                  "null"
               ]
            },
            "rationale": {
               "type": "string"
            },
            "rationale_derivation": {
               "$ref": "#/$defs/Derivation"
            },
            "classification_derivation": {
               "$ref": "#/$defs/Derivation"
            },
            "grounded_on": {
               "description": "The retrieved things this object rests on. For grounding mode retrieval, each entry names a retrieval span; for input_only and none, the single entry is the input itself (source_id input:<invocation_id>, content_hash the dd-input-json-v1 hash).",
               "items": {
                  "$ref": "#/$defs/GroundingRef"
               },
               "type": "array"
            }
         },
         "required": [
            "kind",
            "target",
            "rationale",
            "rationale_derivation",
            "grounded_on"
         ],
         "title": "Recommendation",
         "type": "object"
      },
      "RecommendationKind": {
         "description": "",
         "enum": [
            "controlled_vocabulary",
            "ontology",
            "terminology_unclassified",
            "data_format",
            "field_format"
         ],
         "title": "RecommendationKind",
         "type": "string"
      },
      "SearchedSummary": {
         "additionalProperties": false,
         "description": "What was looked for, so a reader can judge an abstention as well as a recommendation.",
         "properties": {
            "queries": {
               "items": {
                  "type": "string"
               },
               "type": [
                  "array",
                  "null"
               ]
            },
            "snapshot_ref": {
               "description": "Which copy of the registry answered, e.g. snapshot:2026-09-04 or live.",
               "type": [
                  "string",
                  "null"
               ]
            },
            "candidates_retrieved": {
               "type": [
                  "integer",
                  "null"
               ]
            },
            "candidates_qualifying": {
               "type": [
                  "integer",
                  "null"
               ]
            }
         },
         "title": "SearchedSummary",
         "type": "object"
      }
   },
   "$id": "https://w3id.org/data-director/schema/workbench/Recommendations",
   "$schema": "https://json-schema.org/draft/2019-09/schema",
   "additionalProperties": false,
   "description": "The R3 payload. Recommended vocabularies, ontologies and formats.",
   "metamodel_version": "1.11.0",
   "properties": {
      "schema_class": {
         "description": "Type designator: the name of the class this object is an instance of. Discriminates the input and payload unions, and lets a consumer name a payload class it does not recognise.",
         "enum": [
            "Recommendations"
         ],
         "type": "string"
      },
      "items": {
         "items": {
            "$ref": "#/$defs/Recommendation"
         },
         "type": [
            "array",
            "null"
         ]
      },
      "searched": {
         "anyOf": [
            {
               "$ref": "#/$defs/SearchedSummary"
            },
            {
               "type": "null"
            }
         ]
      },
      "grounded_on": {
         "description": "The retrieved things this object rests on. For grounding mode retrieval, each entry names a retrieval span; for input_only and none, the single entry is the input itself (source_id input:<invocation_id>, content_hash the dd-input-json-v1 hash).",
         "items": {
            "$ref": "#/$defs/GroundingRef"
         },
         "type": "array"
      }
   },
   "required": [
      "schema_class",
      "grounded_on"
   ],
   "title": "Recommendations",
   "type": "object",
   "version": "0.7.0"
}
