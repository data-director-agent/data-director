# Default institutional profile. See README.md in this directory for what each key means.
profile_id: default
version: 1
description: Development profile for the workbench; enables every in-tree agent, requires approval for writes.

agents_enabled:
  - quality.reviewer
  - fact.checker
  - r3.standards-advisor
  - stub.abstain
  - hello.world
  - director.stub

actions_requiring_approval:
  - deposit
  - metadata_write

approved_repositories:
  - https://orda.shef.ac.uk
pid_systems:
  - doi
registries:
  fairsharing:
    allowed: true
