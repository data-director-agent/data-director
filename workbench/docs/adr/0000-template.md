# ADR-NNNN: Title

**Status:** Proposed | Accepted | Superseded by ADR-NNNN
**Date:** YYYY-MM-DD

## Context

What question had to be answered, and what constrained the answer.

## Decision

What was decided, stated so that a reader can tell whether a later change violates it.

## Dependencies introduced

| Dependency | Tier | Fallback if this dependency is abandoned |
|---|---|---|
| name | Evaluation/CI path · Runtime path · Rejected | What replaces it, and what internal interface makes that replacement local. If this cell cannot be filled in, the dependency is in the wrong tier (ADR-0006). |

## Consequences

What becomes easier, what becomes harder, and what a future contributor must not do without
revisiting this decision.
